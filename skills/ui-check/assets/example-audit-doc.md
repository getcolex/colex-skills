# Figma divergence backlog

This document tracks every concrete divergence between the Figma design and the live React implementation. Each bullet pairs a specific Figma frame/node with a specific live file:line. The HITL dashboard at `http://localhost:4567/` reads this doc and lets the designer triage; the `/ui-check` skill dispatches Sonnet agents to fix approved bullets.

Format spec: `~/.claude/skills/ui-check/references/audit-bullet-format.md`.

State legend:
- `[ ]` open (not triaged)
- `[x]` approved (dispatch when /ui-check runs)
- `[r]` retry (re-dispatch with note as feedback)
- `[-]` won't fix (silent skip)
- `[?]` TBD (designer pondering)

---

## 9. Audit run — YYYY-MM-DD (cold-start bootstrap)

Source: `mcp__plugin_figma_figma__get_metadata` on the dev canvas, plus per-frame agent comparison against `<live URL>`. Generated by the `/ui-check` skill's bootstrap mode.

### 9A. Frame: <frame name> · <viewport-kind>

- [ ] **9A.1** Hero headline reads "Here is the dream of the person who made your bag." in Bayard Regular 64px in Figma; the live impl uses `text-display` token (clamp 32→82px) which peaks at 82px on desktop — drift in the upper bound. (`LandingClient.tsx:93-95`, Figma `233:1834`)

- [x] **9A.2** Search input in Figma is 560px × 59px; live impl wraps in `max-w-md` (~448px) and uses `min-h-[44px]` instead of a fixed height. (`SearchByName.tsx:33-34`, Figma `233:1831`)

- [x] **9A.3** Footer column heads use Bayard Regular 22px with `tracking-normal` in Figma; live impl applies `tracking-label` (0.2em) instead. (`SiteFooter.tsx:45`, Figma `233:1519`)

### 9B. Frame: <next frame name> · <viewport-kind>

- [ ] **9B.1** ...
